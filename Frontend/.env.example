# Plantilla de variables de entorno para Producción / Frontend (Vite)

# URL del servidor backend en producción (ej. Render, Railway, Fly.io)
# IMPORTANTE: En producción debes colocar la URL completa que incluye /api al final
VITE_API_URL=https://tu-backend-api.onrender.com/api

